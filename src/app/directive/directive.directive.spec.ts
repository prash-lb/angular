import { AlertOnReserveDirective } from './alerte.directive';

describe('DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertOnReserveDirective();
    expect(directive).toBeTruthy();
  });
});
